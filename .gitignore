# secrets — never commit the real env file
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/

# streamlit — keep the committed theme config, ignore everything else (e.g. secrets.toml)
.streamlit/*
!.streamlit/config.toml

# generated artifacts — rebuilt by `ingest` on first run
data/
runs/
